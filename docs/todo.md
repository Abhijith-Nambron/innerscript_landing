# InnerScript Landing TODO

- When the header `Early access` link is selected, make the hero `Claim early access` CTA visually lighten or pulse so the destination is obvious.
- Check the collaborators link/section flow and make sure it points users to the right collaboration signup path.
- Add a first-100 signup countdown or availability indicator once the signup count can be read from the backend/Sheet.
